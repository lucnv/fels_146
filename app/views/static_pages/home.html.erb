<div class="center jumbotron">
  <h2><%= t "views.pages.home.greeting" %></h2>
  <% unless logged_in? %>
    <%= link_to t("views.pages.home.signup_invite"), signup_path,
      class: "btn btn-lg btn-primary" %>
  <% end %>
</div>
